spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mp?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: Tutor(WgZts7u0FWu2/4XX5xTSaQ==)
    password: Tutor(rJtwXQo3ALD8k4ajKjXAlQ==)
    driver-class-name: com.mysql.cj.jdbc.Driver
  devtools:
    restart:
      enabled: true  #设置开启热部署
      additional-paths: src/main/java #重启目录
mybatis-plus:
  global-config:
    db-config:
      id-type: auto
    banner: false
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
server:
  port: 8082
# JWT配置
jwt:
  # 密匙KEY
  secret: JWTSecret
  # HeaderKEY
  tokenHeader: Authorization
  # Token前缀字符
  tokenHead: tutor-it
  # 过期时间 单位秒 1天后过期=86400 7天后过期=604800
  expiration: 86400
  # 配置不需要认证的接口
  antMatchers: /login/**,/favicon.ico

# 加密使用的算法
jasypt:
  encryptor:
    password: tutorIT666
    property:
      prefix: Tutor
#ENC(sZHIFuKC7912feUW5DYEj9Y/D1kgmzIZ1ulgeX/lq22MvpLcoiSvhHF9vL/+6o3M)
#ENC(Gkf/yCY2yVCj6u+SRmfcWlAfLIcTyiif6qZPFTp7GBEJy6fePDCJfSAavrONlCwS)